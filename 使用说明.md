# WorkBuddy 成长任务助手 · 使用说明

本工具用于自动完成 WorkBuddy 成长计划中的任务并领取奖励，获得积分和能量。无需安装 Docker 或 Go，在本机双击即可运行。适合没有 API 服务或网关功能需求、也不想部署运行环境，只想批量完成成长任务积分的用户。

整个文件夹只有一个入口，即 WorkBuddy任务助手.command。登录、挑选任务、批量执行都在其中完成。

## 一、打开

双击 WorkBuddy任务助手.command，会弹出一个终端窗口，第一屏即账号列表：

```
==============================================================================
  WorkBuddy 成长计划任务自动执行脚本
==============================================================================

  请选择要操作的账号
  提示：分批领取，积分不易集中过期

    1)  账号A             uid xxxxxxxx    2 小时前登录
    2)  账号B             uid xxxxxxxx    3 小时前登录
    3)  账号C             uid xxxxxxxx    3 小时前登录

    n)  登录新账号
    j)  所有账号 · 全量完成任务
    k)  所有账号 · 全量完成每日任务
    r)  重新扫描账号
    q)  退出程序
```

各按键的作用如下。输入账号前的数字（如 1、2、3），进入该账号的任务清单；输入 n 登录新账号；输入 j 对所有账号全量完成任务（含每日）；输入 k 对所有账号只完成每日任务；输入 r 重新扫描账号列表；输入 q 退出。

第一次使用时没有任何账号，直接输入 n 登录即可。新账号登录后会自动出现在列表中，无需重启程序。

## 二、登录账号

在第一屏输入 n。屏幕上会显示一条授权链接，浏览器通常会自动打开。在浏览器中完成登录后，回到终端窗口等待，程序会自动检测，一般需要几秒到十几秒。看到“登录成功”即可，凭证会保存在 auths 目录中。登录完成后自动返回账号列表，新账号已在其中。

有几个账号就登录几次，逐个进行。已登录的账号再登一次也不会出错，只是覆盖旧凭证。如果浏览器没有自动打开，把窗口里的链接手动复制到浏览器即可。

## 三、挑选任务

在第一屏输入一个账号数字（如 1），程序会列出该账号的全部任务。清单从上到下分为若干段，只有中间四段带有字母编号：

```
  账号A（uid xxxxxxxx）    能量余额 3
    共 26 个任务 · 还能领 14 个，约 +1250 积分

  — 已经领过的（9 个）——————————————————————
      体验「GLM-5.2」模型 ........... +100 积分 +5 能量  已领过

  — 手动任务（脚本无法完成，3 个）————————————
      体验「公益专家」 .............. +300 积分  做不了

  == a 限时任务（3 个）====================================
   1  参与「校园日」有奖活动 ........ +100 积分  未完成  [9月24日·剩3天]
   2  体验「企鹅教师助手」Buddy 应用 . +50 积分   未完成  [10月10日·剩19天]

  == b 每日任务（1 个）====================================
   4  参与「夜猫子」夜间折扣活动 .... —          已累计 2/3 天

  == d 不限时任务（10 个）================================
   5  体验 WorkBuddy「发现应用」 .... +100 积分  未完成
```

各段的含义如下。“已经领过的”是奖励已到账的任务，不必再处理，这一段没有编号。“手动任务”是脚本无法完成、只能自己在客户端操作的任务，每行下方注明了原因，同样没有编号。“a 限时任务”有截止日期，过期作废，已按最紧迫的排在最前。“b 每日任务”每天可领一次，次日自动重置。“c 已完成待领取任务”是进度已满、只差领奖的任务。“d 不限时任务”没有截止时间，随时可领。

程序每次只显示有内容的段落。例如没有限时任务时，a 段就不会出现。

输入方式如下。做单个任务，输入其编号，如 3。做多个任务，用空格、逗号或顿号分隔，如 1 3 5。做一整段，直接输入段首字母，如 a。整段与单个混合，如 a 3。做全部可执行的任务，输入 0。返回账号列表换号，输入 t。退出，输入 q。

其中字母可以直接当命令使用，这是最省事的方式。例如想清掉所有快过期的任务，直接输入 a 即可。“已经领过的”和“手动任务”两段没有字母、没有编号，无法选中。

输入后程序会把选中的任务再列一遍，然后立即执行，中途没有输出属于正常现象，每个任务需要几秒到十几秒。执行完毕后清单会自动刷新并重新编号，已完成的任务会移动到“已经领过的”一段，因此每次都应以屏幕上的编号为准。

任务名后出现省略号是正常的。清单按固定宽度排版，程序会优先保证状态和截止时间完整显示，空间不足时才会缩短任务名。

## 四、批量执行

在第一屏可以直接批量处理所有账号。输入 k 只完成每日任务，建议每天执行一次，把各账号的每日份额补上，最为常用。输入 j 全量完成所有任务（含每日），适合想一次清空全部可领任务时使用。两者都会一次跑完所有账号，无需逐个操作。

执行时程序会先只读查询每个账号有哪些任务，再逐个执行。跑完先给出一句总述，再逐个账号列出明细。

## 五、关于积分过期

积分有有效期，一次性全部领回可能来不及使用就过期了，因此建议如下。优先处理 a 限时任务段，这一段的任务都会过期，且已按最紧迫的排好顺序，也可以直接输入 a 一次清完；没有进入 a 段的任务不着急，可以留待以后再领。逐个账号处理，选定一个账号做完后输入 t 返回，再选下一个。每天执行一次 k，每日任务每天都能领，是持续获取积分的来源，且当天不领即浪费，一次把所有账号领完最为省事。

## 六、关于夜猫子任务

夜猫子任务的规则是每晚最多计一次、累计三天才算完成，因此执行后可能只显示“进行中，已累计 1/3 天，还差 2 天”。这不是失败。程序会对比执行前后的进度：进度增加，说明当晚这次已计入；进度未变，说明当晚已经领过一次。此任务只在夜间 23 点至次日 8 点之间有效，白天执行会显示跳过。

## 七、如何看懂执行结果

任务执行完毕后，程序会输出一份中文小结。每行最后一个词是任务状态，前面是任务名，后面是原因或收益：

```
  全部 3 个账号的每日任务都做到了

  账号：账号A（uid xxxxxxxx）    能量余额 12
  每日任务 1 个 · 还有 1 项没完成（公益捐赠）

  夜猫子（深夜对话）........ 进行中   已累计 1/3 天，还差 2 天
  创建设计画布.............. 已完成   +100 积分 +5 能量
  领养第一只 Buddy.......... 之前已领 之前已经领过了
  公益捐赠.................. 做不了   要真人操作或真实捐款
  对话活跃 5 次............. 失败     接口返回 3002

  这次一共领到：+100 积分 +5 能量
  任务统计：共 5 个 · 新完成 1 · 之前已领 1 · 跳过 1 · 失败 1
```

状态词共有十种，含义如下。“已完成”表示这次成功完成，奖励已到账。“之前已领”表示早已领过，不会重复领取。“进行中”表示累计型任务还差几天或几次，可次日继续。“领奖失败”表示任务已完成但领奖未成功，下次执行会自动补领。“失败”表示接口报错，需要查看。“做不了”表示需要真人操作或真实捐款，脚本不代做。“未完成”表示这一轮执行了但进度没有推进，下次会自动重试，并非不需要做。“跳过”表示这次确实不需要处理，例如不在夜间时段、官方新增任务需手动完成等。“无此任务”表示该账号没有此任务，仅在批量执行时可能出现。“读不到”表示未能解析该行，通常可以忽略。

每个账号标题下有一行小结，可一眼看出该账号是否成功；批量执行时最上方还有一句总述；最底部是本次领取的积分和能量总计。

## 八、消耗与风险

请认真阅读本节。本工具会向腾讯官方接口发送请求，模拟客户端行为点亮任务进度，并在完成后调用官方领奖接口，把积分记入账号。

需要注意的代价如下。账号存在被封的风险，这是绕过官方客户端的自动化行为，官方用户协议并不支持，目前虽未听说大规模封号，但风险由账号自身承担，建议只用小号尝试。会消耗极少量模型额度，个别任务需要真的发送一条很短的对话消息，其余均为行为上报，不消耗额度。auths 目录中保存的是账号的明文登录凭证，请勿发给他人、勿上传网盘、勿放入任何会自动同步的目录。

本工具不会做的事情如下。不修改电脑的任何系统设置，不安装后台自启，除向腾讯官方接口发送登录和任务请求外，不向任何其他地方发送数据。经核对源码，这些脚本只与 copilot.tencent.com 及 codebuddy.cn、workbuddy.cn 通信，没有额外的外发行为。

## 九、无法自动完成的任务

有三个任务脚本无法代做：体验公益专家需要真的捐款，学生认证需要真人在微信中完成，关注微信服务号是官方后加的任务、脚本暂不支持（在手机微信中关注即可）。这几个任务单独归在“手动任务”一段，注明原因，不会假装完成，也没有编号、无法选中，因此输入 0 或按 j 都不会触及它们。其余任务基本都能自动完成，包括领养 Buddy、桌面端行为链、专家召唤、开学季活动等。

## 十、何时需要重新登录

登录凭证有有效期，从几小时到几天不等。如果运行时报“查询失败”，或汇总里全部是失败，多半是凭证过期了。回到第一屏输入 n 重新登录一次即可，只需几秒。

## 十一、文件夹内容

```
WorkBuddy任务助手/
  WorkBuddy任务助手.command    入口，双击运行
  使用说明.md                  本文件
  LICENSE、README.md           开源协议与项目说明
  auths/                       登录凭证，首次登录后自动生成，勿外传
  scripts/                     程序本体
    任务助手.py                第一屏、任务清单、批量执行
    中文结果.py                日志翻译
    login.py                   登录流程
    task_runner.py             来自上游
    task_common.py             来自上游
    school_open_day_2026.py    来自上游
```

## 十二、常见问题

双击后如果窗口一闪而过或报错看不清，先看清窗口里的报错文字，再对照下表处理。

| 现象 | 原因 | 处理办法 |
|---|---|---|
| 提示没有 Python 3.10+ | 环境问题 | 已装 Homebrew 执行 brew install python@3.13；否则安装 uv（curl -LsSf https://astral.sh/uv/install.sh 后重开终端，再执行 uv python install 3.13） |
| 显示“还没有任何账号” | 没登录 | 输入 n 登录 |
| 提示“查询失败”或读不到任务 | 凭证过期 | 回第一屏输入 n 重新登录 |
| 某任务一直显示“做不了” | 需要真人操作 | 自己手动完成，属正常现象 |
| 夜猫子一直被跳过 | 不在夜间 23 点至 8 点 | 正常，深夜再运行 |
| 一个账号成功、另一个失败 | 失败账号的凭证过期 | 回第一屏用 n 登录该账号，再执行一次 |
| 第一屏输 a 提示要先选账号 | a 是任务分组字母，不是第一屏命令 | 先输账号数字，再输 a |
| 窗口一闪就消失 | command 文件没有执行权限 | 终端执行 chmod +x WorkBuddy任务助手.command |

## 十三、来源与许可

核心代码来自 Sliverkiss/workbuddy2api 的 scripts 目录（MIT 协议，原文见 LICENSE）。我实际是从它的 fork 分支 linguo2625469/workbuddy2api-panel 下载的，其中这几个脚本与上游原始版本一致。

其中 task_runner.py、task_common.py 和 school_open_day_2026.py 三个文件直接取自上游，一行未改，上游更新时直接覆盖即可。在此基础上，我用 AI 把自动做成长任务这部分拆分出来做成独立脚本，并按自己的想法做了调整：整合为统一入口，界面更友好、交互更便捷；重新规划了任务清单的显示逻辑；将执行结果整理为中文清单并细分任务状态；简化了操作流程，例如取消执行前的二次确认、执行完毕后自动返回清单。为此新增了任务助手.py、中文结果.py 和 login.py 三个文件，以及入口脚本和文档。
